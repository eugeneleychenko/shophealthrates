i created a demo campaign with our site on clickflare
https://shophealthrates.com/?gclid={gclid}&wbraid={wbraid}&gbraid={gbraid}&campaignid={campaignid}&adgroupid={adgroupid}&loc_physicall_ms={loc_physical_ms}&loc_interest_ms={loc_interest_ms}&matchtype={matchtype}&network={network}&creative={creative}&keyword={keyword}&placement={placement}&targetid={targetid}&cpid=6a04cfc67e76d10012a65767

here is what the URL would look like with UTMs
since we have to use non redirect the clickflare clickid isnt passed into the UTMs, its passed in the cookies i think, so we would need a script that would grab that clickflare ID and store that into the CRM
with the UTMs from the URL

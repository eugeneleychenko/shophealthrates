# CLAUDE.md

See @AGENTS.md

Claude-specific notes (everything else lives in AGENTS.md):

- This repo is edited by Claude Code both locally and **remotely** — via [claude.ai/code](https://claude.ai/code) and the Telegram `/change`·`/ask` agent (`claude-code-action`). Setup and runbook: [docs/telegram-agent.md](docs/telegram-agent.md).
- **Automated edits must obey the [Protected Tracking Codes](AGENTS.md#protected-tracking-codes--do-not-modify) rules**: targeted edits only (never full-file rewrites), never touch `<head>` tracking blocks or `submitLead()`, never change `tel:+18007581590`.
- For advanced memory features (`CLAUDE.local.md`, `.claude/rules/`, `@import`, `/init`), see https://code.claude.com/docs/en/memory.
